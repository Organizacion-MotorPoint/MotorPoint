name: CI - MotorPoint

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  ci-check:
    runs-on: ubuntu-latest
    
    needs: []

    steps:
      - name: CI Status Check
        run: echo "Frontend and Backend CI workflows are running in parallel"


