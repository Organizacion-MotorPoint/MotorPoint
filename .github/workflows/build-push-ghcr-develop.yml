name: Build & Publish (Staging)

on:
  push:
    branches: [ "develop" ]

permissions:
  contents: read
  packages: write

env:
  IMAGE_REGISTRY: ghcr.io
  IMAGE_OWNER: ${{ toLower(github.repository_owner) }}

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Log in to GHCR
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & push backend (staging)
        uses: docker/build-push-action@v4
        with:
          context: ./backend
          file: ./backend/Dockerfile
          push: true
          tags: |
            ${{ env.IMAGE_REGISTRY }}/${{ env.IMAGE_OWNER }}/motorpoint-backend:develop
            ${{ env.IMAGE_REGISTRY }}/${{ env.IMAGE_OWNER }}/motorpoint-backend:${{ github.sha }}

      - name: Build & push frontend (staging)
        uses: docker/build-push-action@v4
        with:
          context: ./frontend
          file: ./frontend/Dockerfile
          push: true
          tags: |
            ${{ env.IMAGE_REGISTRY }}/${{ env.IMAGE_OWNER }}/motorpoint-frontend:develop
            ${{ env.IMAGE_REGISTRY }}/${{ env.IMAGE_OWNER }}/motorpoint-frontend:${{ github.sha }}
